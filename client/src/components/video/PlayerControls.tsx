/**
 * PlayerControls - Thin wrapper around Video.js built-in controls.
 *
 * Video.js provides a full-featured control bar (play/pause, seek, volume,
 * fullscreen, etc.) out of the box. This file exists as a placeholder for
 * future customisation (e.g. adding a sync indicator, custom overlay buttons).
 *
 * For now, no extra UI is rendered -- all control is handled by the
 * Video.js control bar configured in VideoPlayer.tsx.
 */

export function PlayerControls() {
  // Intentionally empty - Video.js built-in controls are used.
  // Custom overlay controls can be added here in the future.
  return null;
}
